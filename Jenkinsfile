pipeline {
    agent {
        label 'jenkins'
    }

    stages {
        stage('Testing') {
            steps {
                echo "Testing"
            }
        }

	stage('Deploy') {
	    steps {
		echo "Deploy"
	    }
	}
    }
}
